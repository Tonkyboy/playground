from turtle import *
from colorsys import *

tracer(50)
bgcolor("black")
shape("circle")
pensize(1)
h = 0

penup()
for i in range(600):
    h = (h + 0.01) % 1
    color(hsv_to_rgb(h, 1, 1))
    size = 0.1 + i * 0.01
    shapesize(size, size)
    forward(i * 0.5)
    right(59)
    stamp()
done()
